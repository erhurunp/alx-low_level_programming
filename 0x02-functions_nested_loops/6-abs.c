#include "main.h"

/**
 * _abs - computes the absolute value of
 * an integer
 * @r: r is an integer
 *
 * Return: an integer
 */

int _abs(int r)
{
	if (r < 0)
	{
		return (-r);
	}
	if (r > 0)
	{
		return (r);
	}
	else
	{
		return (r);
	}
}
